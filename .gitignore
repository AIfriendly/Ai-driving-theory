# Secrets — never commit exchange credentials
.env

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
dist/
build/
.claude/settings.local.json

# Remotion render output and deps
video/node_modules/
video/out/

# Stray deps from local verification tooling (Playwright etc.) — the web app
# itself has no dependencies and no build step
/node_modules/
